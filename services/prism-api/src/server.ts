import Fastify from "fastify"; import cors from "@fastify/cors"; import multipart from "@fastify/multipart"; import {config} from "./config.js"; import {createSession,getSession,saveSession} from "./repositories/intakeRepository.js"; import {start,decide} from "./orchestrator.js"; import {draftSummary} from "./summary.js"; import {registerUpload} from "./resourcePipeline.js"; import {getResource} from "./storage.js"; import {listPatientResources} from "./repositories/resourceRepository.js"; import {getBinary} from "./binaryStore.js"; import {createGrant,revokeGrant} from "./repositories/resourceAccessRepository.js"; import {resolveForClinician} from "./medbridgeBridge.js"; import {HttpOcrWorkerAdapter} from "./ocr/ocrWorkerAdapter.js"; import {processResource} from "./ocr/processingPipeline.js"; import {normalizeCandidate} from "./clinical/normalizeEvidence.js"; import {saveEvidence,listEvidence} from "./repositories/evidenceRepository.js"; import {evidenceBundle} from "./fhir/bundle.js"; import {BhashiniAdapter} from "./asr/bhashiniAdapter.js";
const app=Fastify({logger:true}); await app.register(cors,{origin:true}); await app.register(multipart,{limits:{fileSize:25*1024*1024,files:1}});
app.get("/health",async()=>({status:"ok",service:"prism-api",persistence:config.databaseUrl?"postgresql":"development-memory",dependencies:{ocr_worker:process.env.OCR_WORKER_URL||"not_configured",bhashini:process.env.BHASHINI_ASR_URL&&process.env.BHASHINI_API_KEY?"configured":"not_configured"}}));
app.post("/v1/intake/sessions",async(req:any)=>{const b=req.body||{};const s=await createSession({patientId:b.patient_id||null,language:b.language,entryPoint:b.entry_point,pathway:b.pathway});start(s);return saveSession(s)});
app.get("/v1/intake/sessions/:id",async(req:any,res)=>{const s=await getSession(req.params.id);return s||res.code(404).send({error:"session_not_found"})});
app.post("/v1/intake/sessions/:id/responses",async(req:any,res)=>{const s=await getSession(req.params.id);if(!s)return res.code(404).send({error:"session_not_found"});const value=req.body?.value;if(!value)return res.code(400).send({error:"value_required"});await decide(s,value,req.body?.input_mode||"text");return saveSession(s)});
app.post("/v1/intake/sessions/:id/voice",async(req:any,res)=>{const s=await getSession(req.params.id);if(!s)return res.code(404).send({error:"session_not_found"});const file=await req.file();if(!file)return res.code(400).send({error:"audio_required"});try{const result=await new BhashiniAdapter().transcribe({audio:await file.toBuffer(),mediaType:file.mimetype,languageHint:s.language});s.language=result.language||s.language;if(result.text)await decide(s,result.text,"voice");await saveSession(s);return{session:s,transcript:result.text,asr:{language:result.language,confidence:result.confidence,provider:result.provider}}}catch(e:any){return res.code(503).send({error:"voice_unavailable",message:e.message})}});
app.get("/v1/intake/sessions/:id/summary",async(req:any,res)=>{const s=await getSession(req.params.id);return s?draftSummary(s):res.code(404).send({error:"session_not_found"})});
app.post("/v1/resources/upload",async(req:any,res)=>{const file=await req.file();if(!file)return res.code(400).send({error:"file_required"});const buffer=await file.toBuffer();const patientId=(req.query as any)?.patient_id||null;return {resource:await registerUpload({filename:file.filename,mimetype:file.mimetype,buffer},patientId),status:"registered",next:"processing_pending"}});
app.get("/v1/patients/:patientId/resources",async(req:any)=>listPatientResources(req.params.patientId));
app.get("/v1/resources/:id",async(req:any,res)=>{const resource=await getResource(req.params.id);return resource||res.code(404).send({error:"resource_not_found"})});
app.get("/v1/resources/:id/download",async(req:any,res)=>{const resource=await getResource(req.params.id);if(!resource)return res.code(404).send({error:"resource_not_found"});const bytes=await getBinary(resource.storageKey);if(!bytes)return res.code(404).send({error:"binary_not_found"});res.type(resource.mediaType);return res.send(bytes)});
app.post("/v1/resources/:id/process",async(req:any,res)=>{const resource=await getResource(req.params.id);if(!resource)return res.code(404).send({error:"resource_not_found"});const bytes=await getBinary(resource.storageKey);const result=await processResource(new HttpOcrWorkerAdapter(),{resourceId:resource.resourceId,mediaType:resource.mediaType,bytes:bytes||undefined,languageHint:(req.query as any)?.language_hint});if(result.status==="completed"){const evidence=[];for(const candidate of result.evidence){const normalized=normalizeCandidate(candidate,{patientId:resource.patientId,resourceId:resource.resourceId,originalResourceId:resource.kind==="original"?resource.resourceId:resource.parentResourceId||undefined});evidence.push(await saveEvidence(normalized))}return{...result,evidence}}return result});
app.get("/v1/patients/:patientId/evidence",async(req:any)=>listEvidence(req.params.patientId,req.query?.category));
app.get("/v1/patients/:patientId/fhir-bundle",async(req:any)=>evidenceBundle(req.params.patientId,await listEvidence(req.params.patientId)));
app.post("/v1/resources/:id/access-grants",async(req:any,res)=>{const b=req.body||{};return createGrant({resourceId:req.params.id,patientId:b.patient_id||null,audience:"medbridge_clinician",purpose:b.purpose||"clinical_review",consentReference:b.consent_reference||null,accessSessionId:b.access_session_id||null,expiresAt:b.expires_at||new Date(Date.now()+30*60*1000).toISOString(),createdBy:b.created_by||null})});
app.delete("/v1/resource-access-grants/:grantId",async(req:any,res)=>{const grant=await revokeGrant(req.params.grantId);return grant||res.code(404).send({error:"grant_not_found"})});
app.post("/v1/medbridge/resources/:id/resolve",async(req:any,res)=>{const b=req.body||{};const result=await resolveForClinician(req.params.id,{accessSessionId:b.access_session_id,patientId:b.patient_id,clinicianId:b.clinician_id});return result.allowed?result:res.code(403).send(result)});
await app.listen({port:config.port,host:"0.0.0.0"});