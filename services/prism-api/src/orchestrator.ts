import type {IntakeSession} from "./types.js";
export const ask=(patient_text:string,options:any[]=[])=>( {type:"ASK",patient_text,input_mode:"BOTH",options} );
export function decide(s:IntakeSession,value:string,inputMode:string){const ctx:any=s.clinicalContext;ctx.turns=[...(ctx.turns||[]),{value,inputMode,at:new Date().toISOString()}];const v=value.toLowerCase();if(!ctx.chief_complaint){ctx.chief_complaint=value;if(/chest|nenju|மார்பு/.test(v)){s.nextAction=ask("When did the chest discomfort begin?");ctx.protocol="chest_pain"}else if(/fever|temperature|காய்ச்சல்/.test(v)){s.nextAction=ask("When did the fever begin?");ctx.protocol="fever"}else{s.nextAction=ask("When did this problem first start?");ctx.protocol="general_hpi"}return s}if(!ctx.onset){ctx.onset=value;s.nextAction=ask("How has it changed since it started?");return s}if(!ctx.progression){ctx.progression=value;s.nextAction=ask("Are there any other symptoms you have noticed?");return s}s.nextAction={type:"COMPLETE_SECTION",patient_text:"Thank you. The main concern is captured. We can continue with relevant health history.",input_mode:"BOTH",options:[]};s.state="history";return s}