import type {IntakeSession} from "./types.js"; import {screenRedFlags} from "./safety.js"; import {questionsFor,recordAnswer} from "./protocolEngine.js";
export const ask=(patient_text:string,options:any[]=[])=>( {type:"ASK",patient_text,input_mode:"BOTH",options} );
export function decide(s:IntakeSession,value:string,inputMode:string){const ctx:any=s.clinicalContext;ctx.turns=[...(ctx.turns||[]),{role:"patient",value,inputMode,at:new Date().toISOString()}];if(!ctx.chief_complaint){ctx.chief_complaint=value;const flag=screenRedFlags(value);ctx.safety_flag=flag;if(flag.level==="emergency"){s.state="triage_required";s.nextAction={type:"TRIAGE_ALERT",patient_text:"Please wait. A staff member needs to review your symptoms immediately.",input_mode:"NONE",options:[]};return s}}const next=questionsFor(s)[0];if(next){recordAnswer(s,next.key,value,inputMode);const following=questionsFor(s)[0];s.nextAction=following?ask(following.text,following.options||[]):{type:"COMPLETE_SECTION",patient_text:"Thank you. Your initial history has been captured.",input_mode:"BOTH",options:[]};}else{s.state="history";s.nextAction={type:"COMPLETE_SECTION",patient_text:"Thank you. Your initial history has been captured.",input_mode:"BOTH",options:[]};}return s}