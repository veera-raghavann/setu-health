import {createHash} from "crypto"; import {registerResource} from "./storage.js"; import {putBinary} from "./binaryStore.js";
export async function registerUpload(file:{filename:string;mimetype:string;buffer:Buffer},patientId:string|null){const sha256=createHash("sha256").update(file.buffer).digest("hex");const storageKey=`resources/original/${sha256}`;await putBinary(storageKey,file.buffer);return registerResource({patientId,originalFilename:file.filename,mediaType:file.mimetype,byteSize:file.buffer.length,sha256,storageKey,kind:"original",parentResourceId:null})}
export async function registerProcessed(input:{parentResourceId:string;filename:string;mediaType:string;bytes:Buffer;patientId:string|null}){const sha256=createHash("sha256").update(input.bytes).digest("hex");const storageKey=`resources/processed/${sha256}`;await putBinary(storageKey,input.bytes);return registerResource({patientId:input.patientId,originalFilename:input.filename,mediaType:input.mediaType,byteSize:input.bytes.length,sha256,storageKey,kind:"processed",parentResourceId:input.parentResourceId})}