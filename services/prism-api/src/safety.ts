export interface SafetyFlag{level:"emergency"|"urgent"|"none";reason:string;action:string}
export function screenRedFlags(text:string):SafetyFlag{const t=text.toLowerCase();if(/(chest pain|pressure in chest).*(breath|sweat|faint)|(stroke|face droop|slurred speech)|unconscious|severe bleeding/.test(t))return{level:"emergency",reason:"Possible time-critical symptom pattern",action:"Route for immediate human triage; do not continue routine intake."};if(/high fever.*(confusion|breath)|severe pain|suicid/.test(t))return{level:"urgent",reason:"Potentially serious symptom pattern",action:"Prompt rapid staff review."};return{level:"none",reason:"No rule-based red flag detected",action:"Continue structured intake."}}