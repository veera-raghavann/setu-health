import type {OcrAdapter} from "./ocrAdapter.js";import {extractEvidence} from "./evidenceExtractor.js";export async function processResource(adapter:OcrAdapter,input:{resourceId:string;mediaType:string;bytes?:Buffer;languageHint?:string}){const ocr=await adapter.extract(input);if(ocr.status!=="completed")return{ocr,evidence:[],status:"failed"};return{ocr,evidence:extractEvidence(ocr),status:"completed"}}