# PRISM ASR

Voice is treated as an input adapter, not a separate clinical workflow.

Audio → ASR provider → transcript + language + confidence → unified conversation engine.

The Bhashini adapter is intentionally credential-driven. No production endpoint or key is committed to the repository.