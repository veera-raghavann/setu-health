import type {AsrAdapter,AsrResult} from "./types.js";
export class BhashiniAdapter implements AsrAdapter{constructor(private baseUrl=process.env.BHASHINI_ASR_URL||"",private apiKey=process.env.BHASHINI_API_KEY||""){}
async transcribe(input:{audio:Buffer;mediaType:string;languageHint?:string}):Promise<AsrResult>{if(!this.baseUrl||!this.apiKey)throw new Error("bhashini_not_configured");const res=await fetch(this.baseUrl,{method:"POST",headers:{"Authorization":`Bearer ${this.apiKey}`,"Content-Type":input.mediaType,"X-Language-Hint":input.languageHint||""},body:input.audio});if(!res.ok)throw new Error(`bhashini_asr_failed_${res.status}`);const raw:any=await res.json();return{text:raw.text||raw.transcript||"",language:raw.language||input.languageHint||"unknown",confidence:Number(raw.confidence||0),provider:"bhashini",raw}}}